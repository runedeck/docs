# Forward Compatibility Fixtures

Each child directory is named for one pinned upstream commit. A fixture belongs here only when Rune intentionally adopts behavior newer than OpenSpec v1.6.0.

Floating branch behavior is not a compatibility contract.
